export default function FilterPanel({ children }) {
  return (
    <div className="grid gap-3 rounded-[1.75rem] border border-white/60 bg-white/80 p-4 shadow-panel">
      {children}
    </div>
  )
}
